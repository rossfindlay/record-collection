# ── Frontend (Vercel) ────────────────────────────────────────────────────────
# URL of the Railway-hosted API server. In local dev defaults to
# http://localhost:8080.
NEXT_PUBLIC_API_URL="https://record-collection-api.up.railway.app"
