# ── Frontend (Vercel) ────────────────────────────────────────────────────────
# Server-side URL of the Railway-hosted API server.
# Used by Next.js rewrites to proxy /api/user/* requests.
# In local dev defaults to http://localhost:8080.
API_URL="https://record-collection-api.up.railway.app"
